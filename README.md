# SOM Color Image Segmentation
focusing on clustering an image into desired number of clusters using an SOM-based approach and then evaluates the quality of the clustering using the Davies-Bouldin Index.
